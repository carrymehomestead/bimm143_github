#NOTES#
  # "option + -" makes a "<-" symbol
  #[] means subsetting. eg: x[4] = 4, the fourth element in the vector  
  #c means you are setting a list of vectors. you need a comma between each value
  #vectors of equal length added to each other will add up individually = "element wise addition"
  #cbind binds columns
  #rbind binds entire tables
  #df$column or df[,#]. df$col[#]